# stisim.github.io

STIsim website. Built with [Quarto](https://quarto.org) and published to GitHub Pages.

## Local preview

```sh
quarto preview
```

## Publish

```sh
./publish
```

This runs `quarto publish gh-pages`, which builds the site and pushes the rendered output to the `gh-pages` branch.
