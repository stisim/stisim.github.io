{
  "name": "stisim-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "serve": "npx --yes http-server _site -p 8081"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0"
  }
}
